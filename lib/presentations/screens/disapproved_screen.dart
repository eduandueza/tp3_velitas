import 'package:flutter/material.dart';

class DisapprovedScreen extends StatelessWidget {
  const DisapprovedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}